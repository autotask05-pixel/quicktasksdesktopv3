{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allows the main QuickTasks window to use the Tauri IPC commands required by the desktop UI.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open", "default"]
}
